#include<iostream>
#include<vector>
using namespace std;
 
bool compare(pair<int,int> p1, pair<int,int> p2){
    return p1.second<p2.second;
}

int fractionalKnapsack(vector<int> val, vector<int> wt, int W){
    int n =val.size();
    for(int i=0;i<n;i++){
        int r=val[i]/wt[i];
        ratio[i]=make_pair9r,i
    }
}

int main(){
    vector<int> val=  {60,100,120};
    vector<int> wt= {10,20,30};
    int W=50;


    return 0; 
}